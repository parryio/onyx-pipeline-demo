**Long demo fixture**

Placeholder for `scanned_book.pdf` – an image-only (or mostly image) PDF used to demonstrate OCR path.

Provenance: (Add public-domain source OR note kept local / not distributed.)

This file may be large and is tracked (or ignored) via Git LFS rules in `.gitattributes`.
